{
  "name": "@delta-mcp/server",
  "version": "0.1.0",
  "description": "MCP2 server runtime with progressive disclosure",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run || true"
  },
  "dependencies": {
    "@delta-mcp/core": "^0.1.0"
  }
}
