{
  "name": "@mcp2/server",
  "version": "0.1.0",
  "description": "MCP2 server runtime with progressive disclosure",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp2/core": "^0.1.0"
  }
}
