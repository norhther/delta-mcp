{
  "name": "@mcp2/client",
  "version": "0.1.0",
  "description": "MCP2 client with progressive disclosure",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run || true"
  },
  "dependencies": {
    "@mcp2/core": "^0.1.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
