{
  "name": "@delta-mcp/cli",
  "version": "0.2.1",
  "license": "MIT",
  "description": "Delta-MCP CLI — inspect, test, and benchmark Delta-MCP/MCP servers",
  "type": "module",
  "bin": {
    "delta-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "!dist/**/*.test.*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/norhther/delta-mcp.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@delta-mcp/client": "^0.2.1",
    "@delta-mcp/core": "^0.2.1"
  }
}
