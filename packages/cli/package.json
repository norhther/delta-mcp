{
  "name": "@delta-mcp/cli",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Delta-MCP CLI — inspect, test, and benchmark Delta-MCP/MCP servers",
  "type": "module",
  "bin": {
    "delta-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@delta-mcp/client": "^0.1.0",
    "@delta-mcp/core": "^0.1.0"
  }
}
